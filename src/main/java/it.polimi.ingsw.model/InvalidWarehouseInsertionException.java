package it.polimi.ingsw.model;
/**
 *Exception called when the insertion doesn't resoect the warehouse rules, or the cell was already full.
 */
public class InvalidWarehouseInsertionException extends Exception{
    //the insertion doesn't respect the warehouse rules, or the cell was already full
}
